apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "llm-classifier.fullname" . }}-test-connection"
  labels:
    {{- include "llm-classifier.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "llm-classifier.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
